# REST-API-Testing-with-Postman
API Testing project using **Postman** to validate REST API endpoints with GET, POST, and DELETE requests. Includes positive and negative test scenarios, response validation, HTTP status code verification, and screenshots of test results. Tested successful requests with **200 OK** and an invalid resource scenario returning **404 Not Found**.
